# CodeAdventurer 全关卡设定方案 v3（边玩边学 · 一语言一玩法）

> 核心理念：**每一关都是一个独立小游戏**，代码就是操作游戏的方式。
> 拒绝"任务简报 + 编辑器 + 判题"的考试感；场景占满屏幕，输入融入场景，
> 写代码 → 看世界变化 → 达成目标通关。
> 每门语言一种游戏类型，绝不套用同一种皮。

---

## 一、总览：一语言 = 一种游戏类型

| # | 语言 | 玩法类型 | 关卡名 | 核心循环 | 学习点 |
|---|------|----------|--------|----------|--------|
| 1 | HTML | 🧱 建造游戏 | 快递分拣 | 码包裹上架 | `ul`/`li` 列表 |
| 2 | CSS | 🎨 变形游戏 | 隐形的信使 | 让信使现身 | `opacity` 透明度 |
| 3 | JavaScript | ✨ RPG 施法 | 三把钥匙 | if 开锁 | `if` + `===` |
| 4 | Python | 🥷 动作跑酷 | Z 字寻宝 | 忍者收星 | `for` 循环 |
| 5 | Vue | ♟️ 卡牌策略 | 能量仪表 | 充能激活卡牌 | 响应式状态 |
| 6 | Java | 🏰 沉浸 RPG | 魔法世界 | 探索·学咒·施法 | 类与对象 |
| 7 | C/C++ | 🏎️ 竞速改装 | 氮气加速 | 调校引擎 | 变量 + `+=` |
| 8 | C# | 🎯 射击动作 | 枪械铭牌 | 装配上膛 | `string` + `int` |
| 9 | Uni-app | 🐹 养成模拟 | 宠物小屋 | 唤醒宠物 | `<view>` 容器 |

> 设计原则：**每种玩法都有一条"目标 → 操作 → 反馈 → 达成"的游戏循环**，
> 代码是玩家手中的"操作手柄"，不是试卷答案。

---

## 二、逐关设定（重点：玩法与游戏循环）

### 关卡 1 · HTML · 建造游戏「快递分拣」

- **游戏类型**：建造 / 整理上架
- **场景**：一间科幻仓库，传送带缓缓送来包裹 📦，旁边是一排空货架
- **叙事**：仓库爆单了！你是唯一的值班建造师。把包裹一件件码上货架，仓库才能恢复运转。
- **目标**：写出 `<ul>` 列表 + 3 个 `<li>`（每个写一个包裹名）
- **游戏循环**：
  1. 玩家在底部输入框写代码（输入即生效）
  2. 写 `<ul>` 的瞬间 → 货架出现；每写一个 `<li>` → 一个包裹"咻"地飞进货架格
  3. 3 个包裹齐 → 货架亮灯、传送带停、仓库灯光变暖 → 通关
- **失败反馈**：不是红字报错，而是"包裹还在传送带上转圈"——货架缺几个就少几个格子
- **学习点**：`ul`/`li` 是"装东西的容器"，一 `<li>` 一物

### 关卡 2 · CSS · 变形游戏「隐形的信使」

- **游戏类型**：视觉变形 / 显隐解谜
- **场景**：暮色城堡庭院，一位信使只剩一团透明轮廓 ✉️
- **叙事**：信使中了隐形咒，只有你掌握的 CSS 能让他重现身形。
- **目标**：把 `.courier` 的 `opacity` 改成 1
- **游戏循环**：
  1. 玩家调整 `opacity` 数值（输入即生效）
  2. 信使随数值从透明逐渐显形（0.3 半透明、0.8 快现身…）
  3. `opacity: 1` 完全现身 → 信使鞠躬、信件飞向远方 → 通关
- **失败反馈**：数值不够时信使保持半透明，下方提示"再调大一点试试"
- **学习点**：`opacity` 0~1 控制透明度，数值即视觉

### 关卡 3 · JavaScript · RPG 施法「三把钥匙」（魔法学院系列）

- **游戏类型**：RPG 解谜 / 条件开锁（魔法学院风格，延续魂类世界观但不复用其界面）
- **场景**：魔法学院地下一层，结界之门上有 3 个发光锁孔 🔑
- **叙事**：钥匙编号是秘密。用 if 判断，让代码自己"认出"正确的那把。
- **目标**：写 `if (key === 2) { openLock(2); }`
- **游戏循环**：
  1. 玩家写 if 语句（输入即生效）
  2. 写对 → 对应锁孔亮金色、门缓缓开启、飘出光芒
  3. 写错编号 → 对应锁孔轻轻颤动（提示性反馈，不惩罚）
- **失败反馈**：锁孔颤动 + "这把钥匙对不上，试试 key 的值"
- **学习点**：`if (条件)` 让代码"做判断"，`===` 是"严格相等"

### 关卡 4 · Python · 动作跑酷「Z 字寻宝」

- **游戏类型**：动作 / 路径收集
- **场景**：3×3 发光网格，3 颗星星排成 Z 字 ⭐
- **叙事**：忍者学院试炼——用循环让分身沿 Z 字路线跑完，收集全部星星。
- **目标**：`for i in range(3):` 循环体里调用 `move()`
- **游戏循环**：
  1. 玩家写 for 循环（输入即生效）
  2. 循环开始时忍者分身出现，每跑一步踩亮一格、吃掉一颗星
  3. 3 步收齐 → 得分 +3、烟火特效 → 通关
- **失败反馈**：步数不够 → 星星剩在原地闪烁；没写 move() → 忍者站着不动
- **学习点**：`for ... in range(n)` = "重复做 n 次"，缩进 = "循环里做的事"

### 关卡 5 · Vue · 卡牌策略「能量仪表」

- **游戏类型**：卡牌策略 / 状态驱动
- **场景**：作战指挥桌，3 张灰暗卡牌 + 一根能量条 ⚡
- **叙事**：小队能量枯竭，卡牌全部失效。给核心状态充能，让整队复苏。
- **目标**：给 `player.energy` 赋一个 ≥ 100 的值
- **游戏循环**：
  1. 玩家给 energy 赋值（输入即生效）
  2. 能量条实时上升；达到阈值时卡牌一张张点亮、抖动、发光
  3. 100 满格 → 全部卡牌激活、桌面震屏 → 通关
- **失败反馈**：能量不足时卡牌保持灰暗，能量条显示当前数值
- **学习点**：Vue 响应式 = 改一个数据，所有依赖它的界面自动刷新

### 关卡 6 · Java · 沉浸 RPG「魔法世界」（重点定制）

- **游戏类型**：沉浸式角色扮演（暗黑魔幻画风，参考魂类氛围但独立世界观）
- **场景**：迷雾森林深处的古堡大厅。地面魔法阵缓缓旋转，四周漂浮符文，
  中央立着一座沉睡的"魔法傀儡"（石像形态，双眼紧闭）
- **叙事**：古堡的守护傀儡沉睡了百年。石碑记载：以「类」为蓝图，为它塑形；
  以「方法」为咒语，令它苏醒。编写 Java，唤醒傀儡，继承古堡的意志。
- **目标**：声明 `class Golem`（魔法傀儡类），类内定义 `wake()` 方法
- **游戏循环**：
  1. 玩家输入 Java 代码（输入即生效）
  2. 写出 `class Golem` → 魔法阵从幽蓝变金色，傀儡轮廓浮现
  3. 定义 `wake()` → 傀儡双眼点亮、石壳碎裂、缓缓站起、魔法阵冲天光柱 → 通关
- **失败反馈**：缺类 → 魔法阵暗淡；缺方法 → 傀儡微微颤动但不醒（有朝向性提示）
- **学习点**：`class` = 对象的"设计蓝图"；方法 = 对象会做的"动作"

### 关卡 7 · C/C++ · 竞速改装「氮气加速」

- **游戏类型**：竞速 / 机械调校
- **场景**：深夜维修站，赛车停在举升机上，速度表归零 🏎️
- **叙事**：决赛 30 秒后开始，引擎却还没点火。声明变量、踩下加速，让它冲出去。
- **目标**：`int speed = 60;` + `speed += 100;`（或 `speed = speed + 100;`）
- **游戏循环**：
  1. 玩家声明 speed（输入即生效）→ 引擎点火、指针微动
  2. 写 `+=` 加速 → 指针猛甩、氮气火焰喷出、赛车震动
  3. 完成两步 → 仪表爆表、引擎轰鸣、维修站灯光亮起 → 通关
- **失败反馈**：缺声明 → 仪表静止；缺加速 → 指针停在低位
- **学习点**：`int` 声明整型变量；`x += n` 是"自己加自己"的简写

### 关卡 8 · C# · 射击动作「枪械铭牌」

- **游戏类型**：射击动作 / 武器装配
- **场景**：战术武器库，工作台上躺着一把新枪，铭牌空白 🔫
- **叙事**：出任务前要登记武器。写上名字、装好弹药，才能上膛出发。
- **目标**：`string weapon = "手枪";` + `int ammo = 30;`
- **游戏循环**：
  1. 玩家声明 weapon（输入即生效）→ 铭牌刻出武器名
  2. 声明 ammo → 弹匣图标出现、子弹一颗颗装入
  3. 两个变量齐 → 铭牌亮金、准星变红、上膛完成 → 通关
- **失败反馈**：缺一个变量 → 对应部分保持空白（铭牌/弹匣）
- **学习点**：`string` 存文字（双引号）、`int` 存数字；变量名要"见名知义"

### 关卡 9 · Uni-app · 养成模拟「宠物小屋」

- **游戏类型**：生活模拟 / 宠物养成
- **场景**：手机屏幕里的电子宠物屋，一只仓鼠在角落呼呼大睡 🐹
- **叙事**：你领养了一只电子仓鼠。用 <view> 为它搭一句问候，它会醒来回应你。
- **目标**：写出内容非空的 `<view>问候语</view>`
- **游戏循环**：
  1. 玩家写 view（输入即生效）
  2. 问候出现 → 仓鼠耳朵一动、睁开眼睛、打出"😊 收到！"气泡
  3. 回应出现 → 宠物开心蹦跳、房间亮起暖光 → 通关
- **失败反馈**：view 为空 → 仓鼠继续睡（提示"它还没听到声音"）
- **学习点**：Uni-app 用 `<view>` 做容器，文字写在里面就显示

---

## 三、通用交互规范（所有关卡统一）

1. **输入即生效**：代码输入框实时驱动场景，无需"运行"按钮
2. **场景即反馈**：写对一点、场景变一点；不是弹窗报错，而是世界在回应
3. **无惩罚式失败**：写错不扣分不报红，场景保持"未完成"状态 + 轻柔的方向性提示
4. **完成挑战**：场景达成目标后，按钮变为可点，弹出星级结算
5. **每关一个游戏**：玩法、场景、叙事、反馈全部按语言特性定制

---

## 四、实现顺序建议

1. **Java · 魔法世界**（用户指定重点，沉浸 RPG 样板）
2. **JavaScript · 三把钥匙**（魔法学院系列衔接）
3. 其余 7 关按语言从易到难批量实现

---

## 五、待确认项

- [ ] Java 魔法世界：古堡/傀儡/魔法阵的设定是否符合你的想象？
- [ ] 各关卡是否想要 2~3 个"知识点递进"的小目标（而非单目标）？
- [ ] 是否需要每关一个"彩蛋"（如通关后的小剧场/成就称号）？