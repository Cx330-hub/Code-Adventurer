# CodeAdventurer 游戏化玩法设计文档

## 核心理念：边玩边学 (Learning Through Play)

**拒绝死板的教程模式**。CodeAdventurer 的每一关都不是"跟着打一遍 Hello World"，而是一个有趣的**独立游戏**。

-   **目标**：让用户在解决游戏谜题的过程中，自然而然地掌握编程概念。
-   **路径**：游戏目标 → 玩家编写代码 → 代码驱动游戏世界 → 达成目标通关。
-   **原则**：每一门语言，都是一种全新的游戏类型（Game Genre）。

---

## 分语言游戏玩法设计 (Gameplay by Language)

### 1. HTML - 建造师 (Builder)
*   **游戏类型**：解谜 / 建造游戏
*   **核心玩法**：玩家扮演一名被困在虚空中的建造师，必须编写正确的 HTML 标签来"搭建"周围的环境。
*   **关卡示例**：
    *   **第1关**：黑暗房间 (Dark Room)。画面全黑（`<body>` 无内容）。任务：编写 `<h1>` 标签点亮房间。
    *   **第2关**：空架子 (Empty Shelf)。一个空的相框轮廓。任务：使用 `<img>` 标签把图片放进去。
    *   **第3关**：门禁系统 (Door Lock)。一扇锁着的门。任务：编写包含 `<form>` 和 `<button>` 的代码来解锁房门。
*   **学习点**：理解 HTML 的**结构**语义，知道标签是用来"搭建"物体的。

### 2. CSS - 调色盘 / 变形记 (Shape Shifter)
*   **游戏类型**：物理益智 / 绘画游戏
*   **核心玩法**：画面上有一个基础元素（方块 `div`）和一个"目标"（Target）。玩家只能修改 CSS 属性，让基础元素变化，直到完全匹配目标。
*   **关卡示例**：
    *   **第1关**：变太阳 (Become the Sun)。目标：一个发光的圆形。玩家需写 `border-radius: 50%` 和 `box-shadow`。
    *   **第2关**：彩虹流动 (Rainbow Flow)。目标：背景彩色渐变且在流动。玩家需写 `linear-gradient` 和 `@keyframes`。
    *   **第3关**：倾斜迷宫 (Tilt Maze)。一个方块需要倾斜特定角度才能通过一道门。玩家需使用 `transform: rotate()`。
*   **学习点**：理解 CSS 如何改变物体的**外观**和**空间位置**。

### 3. JavaScript - 魔法学院 (Magic Academy)
*   **游戏类型**：塔防 / 指令谜题
*   **核心玩法**：玩家扮演一名法师，需要编写 JavaScript 代码（逻辑）来施放魔法、打败涌来的 Bug（怪物）。
*   **关卡示例**：
    *   **第1关**：新手弹幕 (Beginner Barrage)。怪物从一个方向跑来。任务：使用 `setInterval` 让角色持续发射"魔法弹"。
    *   **第2关**：智能锁敌 (Smart Targeting)。怪物从四面八方来。任务：用 `if` 判断距离，只攻击最近的敌人。
    *   **第3关**：大招时刻 (Ultimate Move)。Boss 血量很厚。任务：编写一个循环 `for` 结构，瞬间释放全屏伤害（AOE）。
*   **学习点**：将 JS 的**逻辑控制**（条件、循环）转化为直观的战斗操作。

### 4. Python - 忍者训练 (Ninja Training)
*   **游戏类型**：算法 / 路径游戏
*   **核心玩法**：玩家控制一个忍者在网格地图上移动。玩家编写 Python 代码（算法）来控制忍者的移动轨迹、收集物品。
*   **关卡示例**：
    *   **第1关**：直线冲锋 (Linear Dash)。任务：用一个 `for` 循环让忍者向前走 5 步。
    *   **第2关**：Z 字形寻宝 (Zig-Zag Treasure)。地图上有一排星星。任务：编写循环逻辑，让忍者 Z 字形走位收集所有星星。
    *   **第3关**：躲避陷阱 (Avoid Traps)。地图上有陷阱。任务：用条件判断 `if` 让忍者在危险区域变向。
*   **学习点**：体会 Python 在**算法和流程控制**上的优雅表达。

### 5. Vue - 策略棋盘 (Strategy Board)
*   **游戏类型**：卡牌策略 / 状态管理
*   **核心玩法**：一个简单的回合制卡牌游戏。玩家通过改变一个"核心状态"（State）来触发整个牌组的连锁反应。
*   **关卡示例**：
    *   **第1关**：全场加 Buff (Global Buff)。任务：改变一个 `player.power` 的值，让所有卡牌的攻击力（依赖该值）瞬间刷新。
    *   **第2关**：条件激活 (Conditional Activation)。任务：设置一个 `isAlive` 状态，让部分卡牌根据这个状态显示/隐藏。
*   **学习点**：直观理解 Vue 的**响应式**和数据驱动视图的本质。

### 6. 其他语言（首关已在 js/levels.js 落地）

以下语言的首关已在 `js/levels.js` 中实现，均采用"正则解析用户代码片段 + 预置 JS 场景动画"的**模拟执行**方式（非真实运行环境）：

*   **Java**: 模拟机器人 (Simulation / OOP)，学习类和对象。首关：唤醒机器人（`class Robot` + 方法）。
*   **C/C++**: 底层赛车 (Low-level Racing)，学习内存和指针。首关：启动引擎（`int speed = 数字`）。
*   **C#**: 射击游戏 (Shooter)，学习事件驱动。首关：装填弹药（`int ammo = 数字`）。
*   **Uni-app**: 跨平台生活模拟 (Life Simulator)。首关：你好世界（`<view>` 标签）。

---

## 开发路线图 (Development Roadmap)

1.  **Phase 1: 首页与导航** (已完成)
    *   [x] 完成炫酷首页
    *   [x] 实现语言选择卡片切换
2.  **Phase 2: 游戏框架搭建 (Game Engine)** (数据层已完成，UI 接入待办)
    *   [x] 设计关卡数据结构（目标、初始状态、通关判定）—— 见 `js/levels.js`
    *   [x] 实现 9 种语言各 1 个首关数据与运行时（判题 / 提示 / 重置 / Portal 过渡）—— 见 `js/levels.js`
    *   [ ] 搭建关卡页 UI（任务面板 / 编辑器 textarea / 预览 iframe / 按钮栏），并接入 `index.html`
    *   [ ] 实现代码编辑器与实时预览的通信桥（运行时已含，待页面接入）
3.  **Phase 3: 逐个游戏实现 (Implement Games)** (首关数据已实现)
    *   [x] HTML 建造师、CSS 变形记、JavaScript 魔法学院等 9 种语言首关数据
    *   [ ] 每种语言扩展更多关卡
4.  **Phase 4: 完整循环**
    *   [x] 实现游戏内的提示系统（Hint System）—— 见 `js/levels.js` 的 `showHint`
    *   [x] 实现存档与进度系统 —— localStorage（`progress_{langId}`）
    *   [ ] 关卡页 UI 接入后打磨交互、增加音效等
